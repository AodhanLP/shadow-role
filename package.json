{
  "name": "shadow-role",
  "version": "1.0.0",
  "description": "### Personal repository for the shadow role",
  "main": "index.js",
  "scripts": {
    "echo": "echo \"Hello World!\" && exit 1",
    "gs": "git status",
    "docker-clean": "docker system prune -f && docker image prune -f && docker volume prune -f && docker network prune -f"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AodhanLP/shadow-role.git"
  },
  "author": "AodhanLP",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.3"
  },
  "devDependencies": {
    "cypress": "^13.6.6"
  }
}
